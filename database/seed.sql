-- ═══════════════════════════════════════════════════════════
-- Seed: bootstrap admin account
-- The real admin row is inserted by database/seed.js (Node)
-- because it needs bcrypt + AES encryption from the app layer.
-- This file is intentionally a no-op placeholder so Docker
-- init scripts don't error out.
-- ═══════════════════════════════════════════════════════════
SELECT 1;
